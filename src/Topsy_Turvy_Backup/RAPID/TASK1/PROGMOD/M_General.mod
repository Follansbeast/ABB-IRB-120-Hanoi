MODULE M_General
    
    FUNC num Min (num A, num B)
        if A < B RETURN A;
        RETURN B;
    ENDFUNC
    
    FUNC num Max (num A, num B)
        if A > B RETURN A;
        RETURN B;
    ENDFUNC

ENDMODULE